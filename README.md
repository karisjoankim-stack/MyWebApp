# Python Sandbox & Runner Web App (Neomorphic Edition)

A modern, full-stack web application that allows users to write, run, and analyze Python code directly in the browser. It features a Monaco-based code editor, real-time code execution with output capture, package availability checking, and dynamic rendering of generated media (such as charts or images generated by `matplotlib` or `pillow`).

This edition features a stunning, clean **Neomorphic (soft UI) split-pane landing interface** with dynamic ambient glowing backdrops, alongside a dedicated workspace environment for executing Phenome-Wide Association Studies (PheWAS) using **PheTK (The Phenotype Toolkit)**.

The application is fully containerized using **Docker** and **Docker Compose** for seamless setup and isolated execution environments.

---

## 🚀 Key Features

* **Neomorphic Soft-UI Redesign**: An elegant split-screen landing interface (Left Control Pane, Right Code Canvas Pane) featuring soft extruded/sunken shapes, pill buttons, and a rotating pastel mesh gradient glow.
* **Step-by-Step PheTK Pipeline Workspace**: A structured 4-step wizard interface to upload clinical datasets, run ICD-to-Phecode mappings (supporting US clinical and WHO standard configurations), perform multithreaded association regressions (PheWAS), and view generated Manhattan plots on the fly.
* **Monaco Editor Integration**: Features a rich, IDE-like code editing experience directly in the browser, complete with syntax highlighting.
* **Sandboxed Python Execution**: Safely run Python scripts in a dedicated workspace directory on the backend, capturing stdout, stderr, execution times, and exit codes.
* **Automatic Media Extraction**: Automatically scans the execution environment for generated media files (e.g., `.png`, `.jpg`, `.mp4` generated by data science scripts) and displays them to the user.
* **Package Check Dashboard**: Instantly inspects the server environment to verify if common data science libraries (`numpy`, `pandas`, `matplotlib`, `pillow`, `scipy`, `scikit-learn`, `requests`, `phetk`, `statsmodels`) are installed.
* **Run History**: View, re-run, and browse through past executions and their source code.
* **Dockerized Setup**: Multi-container orchestrations for running both backend API and React frontend simultaneously with live hot-reloading.

---

## 🛠️ Tech Stack

### Frontend
* **Core**: React 19 & React Router 7
* **Build System**: Vite
* **Code Editor**: `@monaco-editor/react`
* **Icons**: `lucide-react`
* **HTTP Client**: `axios`
* **Theme**: Custom Neomorphic Soft-UI CSS

### Backend
* **Runtime**: Node.js & Express
* **Execution System**: Sandboxed process spawning (`child_process`) running `python3`
* **Python Libraries**: `numpy`, `pandas`, `matplotlib`, `pillow`, `scipy`, `scikit-learn`, `requests`, `phetk`, `statsmodels`
* **Storage**: Temp directory manager with auto-pruning to keep disk space usage low.

---

## 🏃 Getting Started

You can run this application either using **Docker Compose** (recommended) or **locally / natively**.

### Option A: Running with Docker Compose (Recommended)
This starts the entire application stack in containers with hot-reloading enabled. Make sure you have Docker installed.

1. **Start the containers**:
   ```bash
   docker-compose up --build
   ```
2. **Access the application**:
   * **Frontend**: [http://localhost:5180](http://localhost:5180)
   * **Backend API**: [http://localhost:3002](http://localhost:3002)

---

### Option B: Running Natively (Without Docker)
Make sure you have Node.js (v18+) and Python 3 installed on your system.

#### 1. Setup the Backend
1. Navigate to the backend directory:
   ```bash
   cd backend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the backend server:
   ```bash
   node index.js
   ```
   *(Running on [http://localhost:3002](http://localhost:3002))*

#### 2. Setup the Frontend
1. Open a new terminal and navigate to the frontend directory:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the dev server:
   ```bash
   npm run dev
   ```
   *(Running on [http://localhost:5180](http://localhost:5180))*

---

## 🔌 API Documentation (Backend)

The backend server exposes the following HTTP endpoints:

| Endpoint | Method | Description |
| :--- | :--- | :--- |
| `/api/run` | `POST` | Executes Python code sent in request body (`{ "code": "..." }`). Returns execution stats, outputs, and any generated media URLs. |
| `/api/packages` | `GET` | Checks and returns the installation status of standard data science packages (`numpy`, `pandas`, etc.) in the backend environment. |
| `/api/history` | `GET` | Fetches a historical list of all recent executions, their source code, run times, and outcomes. |
| `/api/recent-code` | `GET` | Returns the source code of the most recently executed run. |
| `/api/results/:runId/:filename` | `GET` | Serves statically generated output files (like charts or images) for a specific run. |
| `/api/phetk/pipeline/initialize` | `POST` | Initializes a new unique analysis run ID. |
| `/api/phetk/pipeline/upload` | `POST` | Accepts phenotype/genotype CSV text contents and returns parsed headers. |
| `/api/phetk/pipeline/configure` | `POST` | Saves custom mapping configurations to the run session. |
| `/api/phetk/pipeline/step2-map` | `POST` | Spawns python mapping helper to translate ICD codes to Phecodes. |
| `/api/phetk/pipeline/step3-stats` | `POST` | Spawns python regression helper to execute the PheWAS association. |
| `/api/phetk/pipeline/step4-visualize` | `POST` | Spawns python plotting helper to generate the Manhattan plot. |
| `/api/phetk/pipeline/results/:runId` | `GET` | Retrieves full regression details and summaries for a specific run ID. |
| `/api/phetk/pipeline/download/:runId/:filename` | `GET` | Downloads specific run files (e.g. `results.tsv` or `manhattan_plot.png`). |
| `/api/phetk/pipeline/run/:runId` | `DELETE` | Removes a specific run directory recursively from disk. |

---

## 📂 Project Structure

```text
├── backend/
│   ├── index.js          # Express API server & execution orchestrator
│   ├── Dockerfile        # Container build definition for backend
│   └── runs/             # Generated execution workdirs (git-ignored)
├── frontend/
│   ├── src/              # React frontend application
│   ├── Dockerfile        # Container build definition for frontend
│   └── package.json
├── docker-compose.yml    # Main Docker Compose orchestration configuration
└── .gitignore            # Git exclusion rules
```

---

## 🔒 Security Note
*This runner is meant for local experimentation, research, and developer environments. Since it executes arbitrary Python scripts on the host system (or inside a docker container), it should not be exposed directly to the open internet without adding secure container sandboxing/gvisor layers.*
